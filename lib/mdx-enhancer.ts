'use server';

export async function enhanceMDXWithImageData(content: string): Promise<string> {
  // TODO: Pre-process image URLs to include dimensions and blur data
  // For now, return content as-is
  return content;
}